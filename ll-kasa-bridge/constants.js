// Constants
const SECOND = 1000;
const MINUTE = 60 * SECOND;
const HOUR = 60 * MINUTE;
const SUBTYPE_BULB = 'bulb';
const SUBTYPE_LED_STRIP = 'led-strip';
const SUBTYPE_PLUG = 'plug';
const SUBTYPE_SWITCH = 'switch';


const constants = {
  SECOND, 
  MINUTE, 
  HOUR,
  SUBTYPE_BULB,
  SUBTYPE_LED_STRIP,
  SUBTYPE_PLUG,
  SUBTYPE_SWITCH,
  SERVICE_PERIODIC_FILTER: 'Periodic Filter Service'
};

export default constants;